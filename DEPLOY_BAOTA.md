# 宝塔面板 (Baota Panel) 部署指南

## 1. 准备工作

1.  登录宝塔面板。
2.  在 **软件商店** 中搜索并安装 **Node.js版本管理器**。
3.  打开 **Node.js版本管理器**，安装 **Node.js v20** (或 v18) 版本。

## 2. 上传文件

1.  在宝塔面板点击 **文件**。
2.  进入 `/www/wwwroot` 目录。
3.  新建一个文件夹，例如 `handv`。
4.  将本压缩包内的所有文件上传到该文件夹中。
    *   `dist/` (文件夹)
    *   `server/` (文件夹)
    *   `package.json`
    *   `start.sh`

## 3. 安装依赖与启动 (方法 A: 使用宝塔 Node.js 项目功能 - 推荐)

1.  点击面板左侧 **网站** -> **Node项目** -> **添加Node项目**。
2.  填写配置：
    *   **项目目录**: 选择你刚才上传的目录 (例如 `/www/wwwroot/handv`)。
    *   **启动选项**: 选择 `server/index.cjs`。
    *   **项目名称**: 填写 `handv`。
    *   **端口**: `6666` (重要：必须与代码中一致)。
    *   **运行用户**: 建议使用 `www` (如果是默认设置)。
3.  点击 **提交**。
4.  宝塔会自动安装依赖并启动服务。如果状态显示“运行中”，则部署成功。

## 4. 绑定域名 (反向代理)

1.  在刚才创建的 Node 项目列表中，点击 **映射** (或设置中的域名管理)。
2.  输入你的域名 (例如 `oa.example.com`)。
3.  如果需要 HTTPS，在 **SSL** 选项卡中申请 Let's Encrypt 证书。

## 5. 常见问题

### 数据库权限问题
由于使用 SQLite 文件数据库，`server/data` 目录必须有**写入权限**。
1.  在宝塔 **文件** 中，找到 `/www/wwwroot/handv/server/data` 目录。
2.  右键点击 -> **权限**。
3.  确保所有者是 `www` (或者你运行 Node 项目的用户)，并且权限设置为 `755` 或 `777`。
4.  同样检查 `server/data/app.db` 文件和 `server/data/uploads` 目录的权限。

### 上传图片失败
如果上传图片失败，通常也是权限问题。请确保 `server/data/uploads` 目录对运行用户可写。

### 依赖安装失败
如果宝塔自动安装依赖失败，可以点击项目设置中的 **依赖管理** -> **一键安装依赖**，并选择淘宝源 (Registry)。
或者进入终端手动安装：
```bash
cd /www/wwwroot/handv
npm install --omit=dev --registry=https://registry.npmmirror.com
```

### 查看日志
在 Node 项目列表中，点击 **日志** 可以查看服务运行日志，排查报错。
